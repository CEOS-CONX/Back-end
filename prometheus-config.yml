global:
  scrape_interval: 15s # 15초에 한 번씩 매트릭 수집

scrape_configs:
  - job_name: 'server_metric'
    scheme: https
    metrics_path: '/actuator/prometheus'

    static_configs:
      - targets:
          - 'api.conx.co.kr:443'
        labels:
          instance: 'prod_server'